# Security

Report security-sensitive issues privately when possible.

This module is client-side Foundry VTT code. It does not make network requests, store credentials, or intentionally read world data beyond the actor/item data already rendered in the current Foundry client session.
